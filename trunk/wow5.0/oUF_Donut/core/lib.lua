
  --get the addon namespace
  local addon, ns = ...

  --object container
  local cfg = ns.cfg
  local lib = CreateFrame("Frame")
  ns.lib = lib

  ---------------------------------------------
  -- lib functions
  ---------------------------------------------

